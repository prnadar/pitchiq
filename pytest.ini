[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast isolated tests with no network or disk dependencies
    integration: tests that touch external services

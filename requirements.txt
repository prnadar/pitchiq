fastapi>=0.100.0
uvicorn>=0.20.0
httpx>=0.24.0
pydantic>=2.0.0
email-validator>=2.0.0

# Pinned exactly: the committed models in backend/models/saved/ are pickles,
# and unpickling them under a different scikit-learn can silently produce
# invalid results. Retrain (scripts/ensure_models.py) whenever these change.
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
# On Linux the default xgboost wheel drags in CUDA/NCCL (125 MB compressed,
# ~700 MB unpacked) which blows past serverless size limits. xgboost-cpu is the
# same version and module, CPU-only, 5.3 MB — so the pickles still load.
xgboost==3.2.0; sys_platform != "linux"
xgboost-cpu==3.2.0; sys_platform == "linux"
lightgbm==4.6.0
joblib==1.5.3
